# Gap Round 29 Review

## Verdict
- `MATCH`

Current HEAD is `16789dd4817b2813f203376c897e05bb9d21ac16` (`fix: close round 28 design gaps`). I found no remaining implementation/design mismatch in the requested G28 follow-up scope or in the rechecked G27 areas.

## Round 28 Follow-up

- G28-01: `FIXED`
  - DENY logs now start timing before the permission check and emit `category="tool"`, `tool_name`, truncated `params_summary`, `permission_result="denied"`, `duration_ms`, `result_size_chars=0`, and `error` (`myagent/subagent/worker.py:298`-`:327`).
  - Rejected ASK logs emit the same tool-log contract fields with the user-denial error text (`myagent/subagent/worker.py:328`-`:358`).
  - The underlying tool still does not execute for permission DENY or user-denied ASK: existing no-execute assertions remain in `tests/subagent/test_worker.py:100`-`:142` and `:198`-`:240`.
  - Success logging did not regress: successful tool calls still log `permission_result="allowed"`, `duration_ms`, and output size (`myagent/subagent/worker.py:360`-`:377`; `tests/subagent/test_worker.py:293`-`:345`).
  - Focused verification passed: `test_run_logs_permission_deny_tool_contract`, `test_run_logs_user_deny_tool_contract`, and `test_run_logs_successful_tool_execution_contract`.

- G28-02: `FIXED`
  - Dream now wraps the active permission controller in `DreamPermissionController`, a non-interactive least-privilege controller (`myagent/memory/dream.py:34`-`:89`).
  - It silently allows only `memory_write` inside memory roots and optional `read`/`glob` inside memory roots, and denies everything else (`myagent/memory/dream.py:37`, `:59`-`:70`, `:91`-`:125`).
  - `confirm()` returns `False` and is not needed for allowed Dream memory maintenance, so background Dream does not trigger interactive confirmation under default permissions (`myagent/memory/dream.py:72`-`:74`; `tests/memory/test_dream.py:110`-`:164`).
  - Side-effect tools remain denied even when the delegate permission controller is permissive: project `write`, `web_fetch`, and an MCP-like external tool are covered by `tests/memory/test_dream.py:166`-`:197`; by implementation, `bash`, `web_search`, `mcp_read_resource`, `mcp_get_prompt`, `config_set`, and unknown tools also fall through to DENY.
  - `memory_write` still routes through `MemoryWriteTool.execute()` to `MemoryStore.write()` when `context.memory_store` exists (`myagent/tools/builtin/memory_tools.py:42`-`:57`), and `MemoryStore.write()` updates the session write log and `MEMORY.md` index (`myagent/memory/store.py:61`-`:110`). This is covered by `tests/memory/test_dream.py:50`-`:108` and `:159`-`:164`.

## Blocking / Important Gaps

None.

## Minor Gaps / Documentation Gaps

None. README and the implementation plan are synchronized to 250 tests (`README.md:7`; `docs/superpowers/plans/2026-07-03-myagentcli-implementation.md:13`, `:1983`).

## Verified Matching Areas

- Tool logging matches the design and project contracts for refused and successful sub-agent tool calls (`docs/superpowers/specs/2026-07-02-myagentcli-design.md:982`; `AGENTS.md:99`).
- Dream remains silent/background and does not modify project code, matching the Dream principle in the design (`docs/superpowers/specs/2026-07-02-myagentcli-design.md:545`-`:577`; `AGENTS.md:65`).
- Dream still analyzes memory files and recent transcript excerpts before spawning the background sub-agent: memory summaries come from `MemoryStore` reads, transcript excerpts are built before spawn, and the sub-agent receives those excerpts in the prompt (`myagent/memory/dream.py:321`-`:333`, `:521`-`:625`, `:701`-`:709`). Therefore restricting sub-agent `read`/`glob` to memory roots does not block transcript excerpts.
- G27-02 remains fixed: `_startup_mcp_servers()` supports documented `mcpServers`, compatibility `servers`, and top-level array configs; project config overrides user config by server name (`myagent/cli/main.py:435`-`:496`; `tests/cli/test_main.py:76`-`:180`).
- G27-03 remains fixed: `/dream` passes the active session store from `SessionManager`, then falls back to `engine.session_store`, then calls `run()` without a store only when none exists (`myagent/cli/commands.py:123`-`:134`; `tests/cli/test_slash_commands.py:59`-`:73`).
- G27-04 remains fixed: Dream sub-agent spawn passes explicit `tool_context` and `config`, and the context retains live `memory_store`, `tool_registry`, config loader, project context, and MCP clients (`myagent/memory/dream.py:338`-`:346`, `:434`-`:503`; `myagent/cli/main.py:133`-`:177`).
- MCP remains an in-repo hand-written JSON-RPC client over stdio/SSE transport abstractions, consistent with the design (`docs/superpowers/specs/2026-07-02-myagentcli-design.md:437`, `:1067`). `pyproject.toml` has no runtime `mcp` SDK dependency (`pyproject.toml:21`-`:28`), and focused import search found no `from mcp` / external `import mcp` use.

## Local Verification

- `git status --short` -> only pre-existing untracked prior gap reports before this report.
- `git show --stat --oneline --decorate --no-renames 16789dd4817b2813f203376c897e05bb9d21ac16` -> `fix: close round 28 design gaps`; 6 files changed, 320 insertions, 13 deletions.
- `git diff --stat --no-renames f17ff06ab01d5561dddd0f5a0dabbcb7a1f941ec..16789dd4817b2813f203376c897e05bb9d21ac16` -> README, implementation plan, Dream, SubAgentWorker, and related tests only.
- Focused G28 tests: `C:\Users\10176\AppData\Local\Programs\Python\Python314\python.exe -m pytest tests\subagent\test_worker.py::TestSubAgentWorker::test_run_logs_permission_deny_tool_contract tests\subagent\test_worker.py::TestSubAgentWorker::test_run_logs_user_deny_tool_contract tests\subagent\test_worker.py::TestSubAgentWorker::test_run_logs_successful_tool_execution_contract tests\memory\test_dream.py::TestDreamEngine::test_dream_permissions_allow_memory_write_without_confirm tests\memory\test_dream.py::TestDreamEngine::test_dream_permissions_do_not_silently_allow_side_effect_tools -v` -> 5 passed, 2 warnings.
- Dream permission probe -> `memory_read=ALLOW`, `user_memory_glob=ALLOW`, `project_code_read=DENY`, `transcript_read=DENY`, `glob_without_path=DENY`.
- `C:\Users\10176\AppData\Local\Programs\Python\Python314\python.exe -m pytest tests/ -v` -> 250 passed, 2 warnings.
- `C:\Users\10176\AppData\Local\Programs\Python\Python314\python.exe -m ruff check myagent/` -> All checks passed.
- `git diff --check` -> exit 0, no whitespace errors.
- `rg -n '(^|[^.])import mcp|from mcp' myagent tests` -> no external MCP SDK imports found.
- `rg -n '"mcp"|mcp>=' pyproject.toml` -> no runtime `mcp` dependency found.
