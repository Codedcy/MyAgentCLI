# Gap Round 26 Review

## Verdict
- `MATCH` (no Blocking / Important gaps found)

Current HEAD is `df36fe345c8fa87e6eaf5a26f3b6e57e01a69296` / `df36fe3 docs: align error log component contract`, matching the requested commit.

Working tree note: pre-existing untracked paths remain (`.claude/`, prior gap reports including round 25). This review only creates `docs/gap-reports/2026-07-03-gap-round-26.md` and does not modify production code, tests, config, or `all-gaps.json`.

Verification limitation: this environment still cannot run `pytest`, `python`, or `ruff` from PATH. `pytest tests/logging/test_exception_logging_static.py -v`, `python -m pytest ...`, `ruff check myagent/`, and `python --version` all failed because the executables are unavailable; `py --version` failed to start the configured Python launcher target. I therefore could not independently rerun the reported gates. The main Agent's reported `pytest tests/logging/test_exception_logging_static.py -v` result of 4 passed, `pytest tests/ -v` result of 238 passed with 2 warnings, and `ruff check myagent/` pass are noted but not treated as locally rerun evidence.

## Round 25 Follow-up

### G25-01
- **Status**: `FIXED`
- **Evidence**:
  - The error `component` enum is now aligned across the repo instructions, design spec, implementation plan, and static guard.
  - `AGENTS.md:103` documents `llm/tool/agent/mcp/system/memory/subagent`.
  - `docs/superpowers/specs/2026-07-02-myagentcli-design.md:985` documents the same enum.
  - `docs/superpowers/plans/2026-07-03-myagentcli-implementation.md:533` requires all `except` blocks to log structured errors, and `:534` documents the same seven component values.
  - `tests/logging/test_exception_logging_static.py:14` sets `ERROR_COMPONENTS = {"llm", "tool", "agent", "mcp", "system", "memory", "subagent"}`.
  - The new `test_documented_error_components_match_static_guard()` (`tests/logging/test_exception_logging_static.py:161`) scans `AGENTS.md` and the design spec and fails if their documented enums drift from `ERROR_COMPONENTS` (`tests/logging/test_exception_logging_static.py:171`).
- **Test coverage**:
  - Static drift coverage exists for docs vs guard.
  - The existing production-source guard still checks every `ast.ExceptHandler` under `myagent` for structured traceback logging (`tests/logging/test_exception_logging_static.py:133`).
  - Traceback-producing logger calls are still guarded for error metadata (`tests/logging/test_exception_logging_static.py:145`).
  - A behavioral regression test remains for malformed AGENT frontmatter logging structured error metadata (`tests/logging/test_exception_logging_static.py:180`).

### G25-02
- **Status**: `FIXED`
- **Evidence**:
  - The design spec now explicitly states that the strict all-except contract records expected fallback/control-flow exceptions and that future structured fields or filters may reduce operator noise while the current contract prioritizes auditability (`docs/superpowers/specs/2026-07-02-myagentcli-design.md:987`).
  - The implementation plan repeats the same policy as "Expected fallback/control-flow noise" (`docs/superpowers/plans/2026-07-03-myagentcli-implementation.md:535`).
  - The code continues to enforce the stricter auditability-first policy through the all-except static guard rather than relying on an undocumented silent whitelist.
- **Test coverage**:
  - The static guard prevents new unstructured exception handlers from silently reappearing.
  - The documentation drift guard prevents the accepted component enum from diverging again.

## Blocking / Important Gaps

None found.

## Minor Gaps / Documentation Gaps

### G26-M01
- **Severity**: Minor / Documentation Drift
- **Design Requirement**: Repository instructions require documentation to stay synchronized when changes affect project status or testing documentation.
- **Actual Implementation**: README and implementation plan still cite 237 tests, while the current round added an additional static guard test and the main Agent reports `pytest tests/ -v` as 238 passed.
- **Evidence**:
  - `README.md:7` says 237 automated tests passed.
  - `docs/superpowers/plans/2026-07-03-myagentcli-implementation.md:13` and `:1983` still say pytest collects 237 tests.
  - `tests/logging/test_exception_logging_static.py:161` adds the new doc/guard enum drift test, and the user-provided gate report says the full suite is now 238 passed.
- **Impact**: Non-blocking status/documentation drift only. It does not affect runtime behavior, design conformance, or whether the gap loop can stop.
- **Suggested Fix**: Update README and the historical status note in the implementation plan to 238 after a Python-capable environment reruns the suite.

## Verified Matching Areas

- HEAD matches the requested round-25 fix commit.
- G25-01 is closed: AGENTS, design spec, implementation plan, and static guard now share the same expanded error component enum.
- G25-02 is closed: expected fallback/control-flow logging noise is explicitly documented as part of the current auditability-first contract.
- Logging implementation remains aligned with the current contract: `JsonLineFormatter` collects normal `extra={...}` record attributes and `extra_fields`, and emits exception type/message/traceback when `exc_info` is present (`myagent/logging/formatter.py:56`, `:68`, `:83`, `:86`).
- MCP remains aligned with the design direction: in-repo JSON-RPC client, stdio and SSE transport abstractions, `resources/list`, `prompts/list`, and registry/context exposure are present (`myagent/tools/mcp/client.py:55`, `:191`, `:675`, `myagent/cli/main.py:525`, `:530`, `myagent/context/builder.py:433`).
- Dream remains wired for startup and periodic background checks, with `last_dream.json` state, processed transcript tracking, and background sub-agent execution path (`myagent/cli/main.py:165`, `myagent/cli/repl.py:716`, `myagent/memory/dream.py:57`, `:124`, `:199`, `:240`).
- Sub-agent limits and background gating match the design: `max_concurrent` defaults to `min(16, max(1, os.cpu_count() - 2))`, session cap is 1000, and non-goal background speculative work is forced off unless config allows it (`myagent/subagent/pool.py:173`, `:185`, `myagent/tools/builtin/agent_tools.py:43`, `:61`).
- Permissions remain centrally enforced in `AgentEngine._execute_tool()` before tool execution, with unknown tools defaulting to level 3 and `--dangerously-skip-permissions` represented as controller state rather than a tool parameter (`myagent/agent/engine.py:875`, `myagent/permissions/controller.py:32`, `:87`, `:204`, `:319`).
- No new runtime MCP SDK dependency drift was found; `pyproject.toml` runtime dependencies are LiteLLM/Rich/prompt_toolkit/PyYAML/Pydantic/httpx, and built-in skill resources are packaged via `skills/builtin/**/*`.

## Suggested Next Fix Scope

None.
